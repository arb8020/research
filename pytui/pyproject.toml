[project]
name = "pytui"
version = "0.1.0"
description = "Elm-style Python TUI framework. Zero deps, differential rendering, pure functions."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
