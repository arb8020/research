[project]
name = "rollouts"
version = "0.1.0"
description = "Lightweight framework for LLM evaluation with vision support"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "dacite>=1.8.0",
]

[project.optional-dependencies]
# Deployment (GPU provisioning + server management)
deploy = [
    "bifrost @ git+https://github.com/arb8020/research.git#subdirectory=bifrost",
    "broker @ git+https://github.com/arb8020/research.git#subdirectory=broker",
]

# Development
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
